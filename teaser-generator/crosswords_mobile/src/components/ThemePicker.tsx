/**
 * src/components/ThemePicker.tsx
 * ---------------------------------------------
 * Horizontal list of available themes. Shows a gradient preview and updates the
 * active theme in the UI store when tapped.
 */

import React from 'react';

export default function ThemePicker(): React.JSX.Element | null {
  // Themes are locked to a single default; hide the picker.
  return null;
}

